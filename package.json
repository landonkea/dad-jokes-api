{
  "name": "dad-jokes-api",
  "version": "1.0.0",
  "description": "The most giggle-worthy dad jokes platform. Because puns are a parent's weapon.",
  "private": true,
  "scripts": {
    "setup": "npm install && cd server && npm install && cd ../client && npm install",
    "db:init": "cd server && npx ts-node src/db/init.ts",
    "db:seed": "cd server && npx ts-node src/db/seed.ts",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "dev": "npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
